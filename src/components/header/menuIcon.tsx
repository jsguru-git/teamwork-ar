import * as React from 'react'
import { View, Icon } from 'native-base';

const MenuIcon = () => {
    return(
        <Icon name='menu' style={{paddingRight: 15, color: '#fff'}}/>
    )
}

export default MenuIcon